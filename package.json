{
  "name": "resume",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:varzy/resume.git",
  "author": "zy <varzy@live.com>",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --write --single-quote ./**/*.{md,js,css}",
    "replace": "node replace.js",
    "update": "npm run replace && git add . && git commit -m \"Update Resume.\" && git push"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  }
}
